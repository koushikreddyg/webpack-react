import { defineMessages } from "react-intl";

const rootPath = "app";

export default defineMessages({
  title: {
    id: `${rootPath}.title`,
    defaultMessage: "Koushik"
  },
  buttonName: {
    id: `${rootPath}.buttonName`,
    defaultMessage: "click me!"
  },
  dummyName: {
    id: `${rootPath}.dummyName`,
    defaultMessage: "click me Dummy!"
  }
});
