import { sendRequest } from "../../networking";

test("should fetch users", () => {
  sendRequest({
    url: "",
    responseType: "",
    mapper: "",
    method: ""
  }).then(resp => console.log(resp));
});
